export class SessionManager {
  constructor() {
  }

  //async initiate(entry) {
    //const { meta, data } = entry
    //const repository = this.portal.get(meta.model)
    //await repository.add(data)
  //}
}
